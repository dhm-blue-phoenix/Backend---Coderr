# Third-party
from django.apps import AppConfig


class OffersConfig(AppConfig):
    """
    App configuration for the offers_app.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "offers_app"
    label = "offers_app"
